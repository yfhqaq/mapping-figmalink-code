[
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=16649-19873",
    "label": "React",
    "language": "typescript",
    "component": "Tabs",
    "source": "https://github.com/yfhqaq/mapping-figmalink-code/blob/main/mapping-mvp/src/components/Tab.tsx",
    "sourceLocation": {
      "line": 20
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst tabs = figma.currentLayer.__properties__.children([\"Tab\"])\nconst __props = {}\nif (tabs && tabs.type !== 'ERROR') {\n  __props[\"tabs\"] = tabs\n}\n\nexport default { ...figma.tsx`<Tabs {...(props as any)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "tabs": {
          "kind": "children",
          "args": {
            "layers": [
              "Tab"
            ]
          }
        }
      },
      "imports": [
        "import { Tabs } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=13660-9415",
    "label": "React",
    "language": "typescript",
    "component": "Tab",
    "source": "https://github.com/yfhqaq/mapping-figmalink-code/blob/main/mapping-mvp/src/components/Tab.tsx",
    "sourceLocation": {
      "line": 8
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst label = figma.currentLayer.__properties__.string('Label')\nconst selected = figma.currentLayer.__properties__.enum('选中', {\n\"Yes\": true,\n\"No\": false})\nconst variant = figma.currentLayer.__properties__.enum('类型', {\n\"常规 Tab\": 'regular'})\nconst __props = {}\nif (label && label.type !== 'ERROR') {\n  __props[\"label\"] = label\n}\nif (selected && selected.type !== 'ERROR') {\n  __props[\"selected\"] = selected\n}\nif (variant && variant.type !== 'ERROR') {\n  __props[\"variant\"] = variant\n}\n\nexport default { ...figma.tsx`<Tab${_fcc_renderReactProp('label', label)}${_fcc_renderReactProp('selected', selected)}${_fcc_renderReactProp('variant', variant)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "label": {
          "kind": "string",
          "args": {
            "figmaPropName": "Label"
          }
        },
        "selected": {
          "kind": "enum",
          "args": {
            "figmaPropName": "选中",
            "valueMapping": {
              "Yes": true,
              "No": false
            }
          }
        },
        "variant": {
          "kind": "enum",
          "args": {
            "figmaPropName": "类型",
            "valueMapping": {
              "常规 Tab": "regular"
            }
          }
        }
      },
      "imports": [
        "import { Tab } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/7CotWdepNuocaUyrN7Mpjy/V4.3.0-%E4%BA%91%E4%BB%93%E7%B4%A2%E8%B5%94?node-id=YOUR_SPT_TABLE_NODE_ID",
    "label": "React",
    "language": "typescript",
    "component": "SptTable",
    "source": "https://github.com/yfhqaq/mapping-figmalink-code/blob/main/mapping-mvp/src/components/gmesh-ui/src/components/SptTable/index.tsx",
    "sourceLocation": {
      "line": 1072
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nexport default figma.tsx`<SptTable {...(props as SptTableProps)}/>`\n",
    "templateData": {
      "props": {},
      "imports": [
        "import { SptTable } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/7CotWdepNuocaUyrN7Mpjy/V4.3.0-%E4%BA%91%E4%BB%93%E7%B4%A2%E8%B5%94?node-id=25-655",
    "label": "React",
    "language": "typescript",
    "component": "SptSearchText",
    "source": "https://github.com/yfhqaq/mapping-figmalink-code/blob/main/mapping-mvp/src/components/gmesh-ui/src/components/SptSearchText/index.tsx",
    "sourceLocation": {
      "line": 14
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst label = figma.currentLayer.__properties__.string('placeholder')\nconst placeholder = figma.currentLayer.__properties__.string('text')\nconst __props = {}\nif (label && label.type !== 'ERROR') {\n  __props[\"label\"] = label\n}\nif (placeholder && placeholder.type !== 'ERROR') {\n  __props[\"placeholder\"] = placeholder\n}\n\nexport default { ...figma.tsx`<SptSearchText {...(props as any)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "label": {
          "kind": "string",
          "args": {
            "figmaPropName": "placeholder"
          }
        },
        "placeholder": {
          "kind": "string",
          "args": {
            "figmaPropName": "text"
          }
        }
      },
      "imports": [
        "import { SptSearchText } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/7CotWdepNuocaUyrN7Mpjy/V4.3.0-%E4%BA%91%E4%BB%93%E7%B4%A2%E8%B5%94?node-id=25-651",
    "label": "React",
    "language": "typescript",
    "component": "SptSearchSelect",
    "source": "https://github.com/yfhqaq/mapping-figmalink-code/blob/main/mapping-mvp/src/components/gmesh-ui/src/components/SptSearchSelect/index.tsx",
    "sourceLocation": {
      "line": 16
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst label = figma.currentLayer.__properties__.string('标题名称')\nconst placeholder = figma.currentLayer.__properties__.string('text')\nconst __props = {}\nif (label && label.type !== 'ERROR') {\n  __props[\"label\"] = label\n}\nif (placeholder && placeholder.type !== 'ERROR') {\n  __props[\"placeholder\"] = placeholder\n}\n\nexport default { ...figma.tsx`<SptSearchSelect {...(props as any)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "label": {
          "kind": "string",
          "args": {
            "figmaPropName": "标题名称"
          }
        },
        "placeholder": {
          "kind": "string",
          "args": {
            "figmaPropName": "text"
          }
        }
      },
      "imports": [
        "import { SptSearchSelect } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/7CotWdepNuocaUyrN7Mpjy/V4.3.0-%E4%BA%91%E4%BB%93%E7%B4%A2%E8%B5%94?node-id=25-653",
    "label": "React",
    "language": "typescript",
    "component": "SptSearchDateRangePicker",
    "source": "https://github.com/yfhqaq/mapping-figmalink-code/blob/main/mapping-mvp/src/components/gmesh-ui/src/components/SptSearchDateRangePicker/index.tsx",
    "sourceLocation": {
      "line": 9
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst label = figma.currentLayer.__properties__.string('日期')\nconst __props = {}\nif (label && label.type !== 'ERROR') {\n  __props[\"label\"] = label\n}\n\nexport default { ...figma.tsx`<SptSearchDateRangePicker {...(props as any)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "label": {
          "kind": "string",
          "args": {
            "figmaPropName": "日期"
          }
        }
      },
      "imports": [
        "import { SptSearchDateRangePicker } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/7CotWdepNuocaUyrN7Mpjy/V4.3.0-%E4%BA%91%E4%BB%93%E7%B4%A2%E8%B5%94?node-id=YOUR_SPT_PAGE_CONTAINER_NODE_ID",
    "label": "React",
    "language": "typescript",
    "component": "SptPageContainer",
    "source": "https://github.com/yfhqaq/mapping-figmalink-code/blob/main/mapping-mvp/src/components/gmesh-ui/src/components/SptPageContainer/index.tsx",
    "sourceLocation": {
      "line": 272
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst tabs = figma.currentLayer.__properties__.children([\"Tab\"])\nconst __props = {}\nif (tabs && tabs.type !== 'ERROR') {\n  __props[\"tabs\"] = tabs\n}\n\nexport default { ...figma.tsx`<SptPageContainer {...(props as unknown as SptPageContainerProps)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "tabs": {
          "kind": "children",
          "args": {
            "layers": [
              "Tab"
            ]
          }
        }
      },
      "imports": [
        "import { SptPageContainer } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=16586-179850",
    "label": "React",
    "language": "typescript",
    "component": "Button",
    "source": "https://github.com/yfhqaq/mapping-figmalink-code/blob/main/mapping-mvp/src/components/Button.tsx",
    "sourceLocation": {
      "line": 13
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst label = figma.currentLayer.__properties__.string('按钮')\nconst type = figma.currentLayer.__properties__.enum('类型', {\n\"一级按钮\": 'primary',\n\"二级按钮\": 'default',\n\"白底按钮\": 'default',\n\"虚线按钮\": 'dashed',\n\"文本按钮\": 'text',\n\"链接\": 'link',\n\"图标按钮\": 'default'})\nconst size = figma.currentLayer.__properties__.enum('尺寸', {\n\"正常\": 'middle',\n\"小尺寸\": 'small'})\nconst kind = figma.currentLayer.__properties__.enum('种类', {\n\"标准\": 'standard',\n\"成功\": 'success',\n\"失败\": 'error'})\nconst disabled = figma.currentLayer.__properties__.enum('禁用', {\n\"on\": true,\n\"off\": false})\nconst loading = figma.currentLayer.__properties__.enum('加载中', {\n\"on\": true,\n\"off\": false})\nconst iconOnly = figma.currentLayer.__properties__.enum('类型', {\n\"一级按钮\": false,\n\"二级按钮\": false,\n\"白底按钮\": false,\n\"虚线按钮\": false,\n\"文本按钮\": false,\n\"链接\": false,\n\"图标按钮\": true})\nconst __props = {}\nif (label && label.type !== 'ERROR') {\n  __props[\"label\"] = label\n}\nif (type && type.type !== 'ERROR') {\n  __props[\"type\"] = type\n}\nif (size && size.type !== 'ERROR') {\n  __props[\"size\"] = size\n}\nif (kind && kind.type !== 'ERROR') {\n  __props[\"kind\"] = kind\n}\nif (disabled && disabled.type !== 'ERROR') {\n  __props[\"disabled\"] = disabled\n}\nif (loading && loading.type !== 'ERROR') {\n  __props[\"loading\"] = loading\n}\nif (iconOnly && iconOnly.type !== 'ERROR') {\n  __props[\"iconOnly\"] = iconOnly\n}\n\nexport default { ...figma.tsx`<Button${_fcc_renderReactProp('label', label)}${_fcc_renderReactProp('type', type)}${_fcc_renderReactProp('size', size)}${_fcc_renderReactProp('kind', kind)}${_fcc_renderReactProp('disabled', disabled)}${_fcc_renderReactProp('loading', loading)}${_fcc_renderReactProp('iconOnly', iconOnly)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "label": {
          "kind": "string",
          "args": {
            "figmaPropName": "按钮"
          }
        },
        "type": {
          "kind": "enum",
          "args": {
            "figmaPropName": "类型",
            "valueMapping": {
              "一级按钮": "primary",
              "二级按钮": "default",
              "白底按钮": "default",
              "虚线按钮": "dashed",
              "文本按钮": "text",
              "链接": "link",
              "图标按钮": "default"
            }
          }
        },
        "size": {
          "kind": "enum",
          "args": {
            "figmaPropName": "尺寸",
            "valueMapping": {
              "正常": "middle",
              "小尺寸": "small"
            }
          }
        },
        "kind": {
          "kind": "enum",
          "args": {
            "figmaPropName": "种类",
            "valueMapping": {
              "标准": "standard",
              "成功": "success",
              "失败": "error"
            }
          }
        },
        "disabled": {
          "kind": "enum",
          "args": {
            "figmaPropName": "禁用",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        },
        "loading": {
          "kind": "enum",
          "args": {
            "figmaPropName": "加载中",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        },
        "iconOnly": {
          "kind": "enum",
          "args": {
            "figmaPropName": "类型",
            "valueMapping": {
              "一级按钮": false,
              "二级按钮": false,
              "白底按钮": false,
              "虚线按钮": false,
              "文本按钮": false,
              "链接": false,
              "图标按钮": true
            }
          }
        }
      },
      "imports": [
        "import { Button } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  }
]