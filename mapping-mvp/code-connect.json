[
  {
    "figmaNode": "https://www.figma.com/design/yPZWrcV5aQbUjBooPLLBvB/%E4%BE%9B%E5%BA%94%E9%93%BE%E4%B8%9A%E5%8A%A1-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v2.0.0?node-id=2692-9954",
    "label": "React",
    "language": "typescript",
    "component": "Upload",
    "source": "",
    "sourceLocation": {
      "line": 10
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst size = figma.currentLayer.__properties__.enum('Size', {\n\"Small\": 'small',\n\"Middle\": 'middle',\n\"Large\": 'large'})\nconst disabled = figma.currentLayer.__properties__.boolean('Disabled')\nconst multiple = figma.currentLayer.__properties__.boolean('Multiple')\nconst accept = figma.currentLayer.__properties__.string('Accept')\nconst label = figma.currentLayer.__properties__.string('Label')\nconst __props = {}\nif (size && size.type !== 'ERROR') {\n  __props[\"size\"] = size\n}\nif (disabled && disabled.type !== 'ERROR') {\n  __props[\"disabled\"] = disabled\n}\nif (multiple && multiple.type !== 'ERROR') {\n  __props[\"multiple\"] = multiple\n}\nif (accept && accept.type !== 'ERROR') {\n  __props[\"accept\"] = accept\n}\nif (label && label.type !== 'ERROR') {\n  __props[\"label\"] = label\n}\n\nexport default { ...figma.tsx`<Upload${_fcc_renderReactProp('size', size)}${_fcc_renderReactProp('disabled', disabled)}${_fcc_renderReactProp('multiple', multiple)}${_fcc_renderReactProp('accept', accept)}${_fcc_renderReactProp('label', label)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "size": {
          "kind": "enum",
          "args": {
            "figmaPropName": "Size",
            "valueMapping": {
              "Small": "small",
              "Middle": "middle",
              "Large": "large"
            }
          }
        },
        "disabled": {
          "kind": "boolean",
          "args": {
            "figmaPropName": "Disabled"
          }
        },
        "multiple": {
          "kind": "boolean",
          "args": {
            "figmaPropName": "Multiple"
          }
        },
        "accept": {
          "kind": "string",
          "args": {
            "figmaPropName": "Accept"
          }
        },
        "label": {
          "kind": "string",
          "args": {
            "figmaPropName": "Label"
          }
        }
      },
      "imports": [
        "import { Upload } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/YOUR_FILE_KEY?node-id=YOUR_TABLE_NODE_ID",
    "label": "React",
    "language": "typescript",
    "component": "Table",
    "source": "",
    "sourceLocation": {
      "line": 10
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst size = figma.currentLayer.__properties__.enum('Size', {\n\"Small\": 'small',\n\"Middle\": 'middle',\n\"Large\": 'large'})\nconst bordered = figma.currentLayer.__properties__.boolean('Bordered')\nconst pagination = figma.currentLayer.__properties__.boolean('Pagination')\nconst loading = figma.currentLayer.__properties__.boolean('Loading')\nconst emptyText = figma.currentLayer.__properties__.string('Empty Text')\nconst __props = {}\nif (size && size.type !== 'ERROR') {\n  __props[\"size\"] = size\n}\nif (bordered && bordered.type !== 'ERROR') {\n  __props[\"bordered\"] = bordered\n}\nif (pagination && pagination.type !== 'ERROR') {\n  __props[\"pagination\"] = pagination\n}\nif (loading && loading.type !== 'ERROR') {\n  __props[\"loading\"] = loading\n}\nif (emptyText && emptyText.type !== 'ERROR') {\n  __props[\"emptyText\"] = emptyText\n}\n\nexport default { ...figma.tsx`<Table {...(props as TableProps)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "size": {
          "kind": "enum",
          "args": {
            "figmaPropName": "Size",
            "valueMapping": {
              "Small": "small",
              "Middle": "middle",
              "Large": "large"
            }
          }
        },
        "bordered": {
          "kind": "boolean",
          "args": {
            "figmaPropName": "Bordered"
          }
        },
        "pagination": {
          "kind": "boolean",
          "args": {
            "figmaPropName": "Pagination"
          }
        },
        "loading": {
          "kind": "boolean",
          "args": {
            "figmaPropName": "Loading"
          }
        },
        "emptyText": {
          "kind": "string",
          "args": {
            "figmaPropName": "Empty Text"
          }
        }
      },
      "imports": [
        "import { Table } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=16649-19873",
    "label": "React",
    "language": "typescript",
    "component": "Tabs",
    "source": "",
    "sourceLocation": {
      "line": 20
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst tabs = figma.currentLayer.__properties__.children([\"Tab\"])\nconst __props = {}\nif (tabs && tabs.type !== 'ERROR') {\n  __props[\"tabs\"] = tabs\n}\n\nexport default { ...figma.tsx`<Tabs${_fcc_renderReactProp('tabs', tabs)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "tabs": {
          "kind": "children",
          "args": {
            "layers": [
              "Tab"
            ]
          }
        }
      },
      "imports": [
        "import { Tabs } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=13660-9415",
    "label": "React",
    "language": "typescript",
    "component": "Tab",
    "source": "",
    "sourceLocation": {
      "line": 8
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst label = figma.currentLayer.__properties__.string('Label')\nconst selected = figma.currentLayer.__properties__.enum('选中', {\n\"Yes\": true,\n\"No\": false})\nconst variant = figma.currentLayer.__properties__.enum('类型', {\n\"常规 Tab\": 'regular'})\nconst __props = {}\nif (label && label.type !== 'ERROR') {\n  __props[\"label\"] = label\n}\nif (selected && selected.type !== 'ERROR') {\n  __props[\"selected\"] = selected\n}\nif (variant && variant.type !== 'ERROR') {\n  __props[\"variant\"] = variant\n}\n\nexport default { ...figma.tsx`<Tab${_fcc_renderReactProp('label', label)}${_fcc_renderReactProp('selected', selected)}${_fcc_renderReactProp('variant', variant)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "label": {
          "kind": "string",
          "args": {
            "figmaPropName": "Label"
          }
        },
        "selected": {
          "kind": "enum",
          "args": {
            "figmaPropName": "选中",
            "valueMapping": {
              "Yes": true,
              "No": false
            }
          }
        },
        "variant": {
          "kind": "enum",
          "args": {
            "figmaPropName": "类型",
            "valueMapping": {
              "常规 Tab": "regular"
            }
          }
        }
      },
      "imports": [
        "import { Tab } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=23715-10452",
    "label": "React",
    "language": "typescript",
    "component": "SidebarNav",
    "source": "",
    "sourceLocation": {
      "line": 8
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst app = figma.currentLayer.__properties__.enum('应用', {\n\"Gmesh\": 'Gmesh',\n\"SEVC\": 'SEVC',\n\"CRM\": 'CRM',\n\"CSM\": 'CSM',\n\"Developer\": 'Developer'})\nconst secondLevelExpanded = figma.currentLayer.__properties__.enum('二级导航展开', {\n\"on\": true,\n\"off\": false})\nconst firstSecondExpanded = figma.currentLayer.__properties__.enum('一二级导航展开', {\n\"on\": true,\n\"off\": false})\nconst __props = {}\nif (app && app.type !== 'ERROR') {\n  __props[\"app\"] = app\n}\nif (secondLevelExpanded && secondLevelExpanded.type !== 'ERROR') {\n  __props[\"secondLevelExpanded\"] = secondLevelExpanded\n}\nif (firstSecondExpanded && firstSecondExpanded.type !== 'ERROR') {\n  __props[\"firstSecondExpanded\"] = firstSecondExpanded\n}\n\nexport default { ...figma.tsx`<SidebarNav${_fcc_renderReactProp('app', app)}${_fcc_renderReactProp('secondLevelExpanded', secondLevelExpanded)}${_fcc_renderReactProp('firstSecondExpanded', firstSecondExpanded)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "app": {
          "kind": "enum",
          "args": {
            "figmaPropName": "应用",
            "valueMapping": {
              "Gmesh": "Gmesh",
              "SEVC": "SEVC",
              "CRM": "CRM",
              "CSM": "CSM",
              "Developer": "Developer"
            }
          }
        },
        "secondLevelExpanded": {
          "kind": "enum",
          "args": {
            "figmaPropName": "二级导航展开",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        },
        "firstSecondExpanded": {
          "kind": "enum",
          "args": {
            "figmaPropName": "一二级导航展开",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        }
      },
      "imports": [
        "import { SidebarNav } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=23715-10623",
    "label": "React",
    "language": "typescript",
    "component": "SidebarNavItem",
    "source": "",
    "sourceLocation": {
      "line": 29
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst level = 'level1'\nconst selected = figma.currentLayer.__properties__.enum('选中', {\n\"on\": true,\n\"off\": false})\nconst __props = {}\nif (level && level.type !== 'ERROR') {\n  __props[\"level\"] = level\n}\nif (selected && selected.type !== 'ERROR') {\n  __props[\"selected\"] = selected\n}\n\nexport default { ...figma.tsx`<SidebarNavItem${_fcc_renderReactProp('level', level)}${_fcc_renderReactProp('selected', selected)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "level": "level1",
        "selected": {
          "kind": "enum",
          "args": {
            "figmaPropName": "选中",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        }
      },
      "imports": [
        "import { SidebarNavItem } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=23846-13616",
    "label": "React",
    "language": "typescript",
    "component": "SidebarNavItem",
    "source": "",
    "sourceLocation": {
      "line": 29
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst level = 'level1'\nconst selected = figma.currentLayer.__properties__.enum('选中', {\n\"on\": true,\n\"off\": false})\nconst __props = {}\nif (level && level.type !== 'ERROR') {\n  __props[\"level\"] = level\n}\nif (selected && selected.type !== 'ERROR') {\n  __props[\"selected\"] = selected\n}\n\nexport default { ...figma.tsx`<SidebarNavItem${_fcc_renderReactProp('level', level)}${_fcc_renderReactProp('selected', selected)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "level": "level1",
        "selected": {
          "kind": "enum",
          "args": {
            "figmaPropName": "选中",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        }
      },
      "imports": [
        "import { SidebarNavItem } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=23715-10686",
    "label": "React",
    "language": "typescript",
    "component": "SidebarNavItem",
    "source": "",
    "sourceLocation": {
      "line": 29
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst level = 'level2'\nconst expanded = figma.currentLayer.__properties__.enum('展开', {\n\"on\": true,\n\"off\": false})\nconst __props = {}\nif (level && level.type !== 'ERROR') {\n  __props[\"level\"] = level\n}\nif (expanded && expanded.type !== 'ERROR') {\n  __props[\"expanded\"] = expanded\n}\n\nexport default { ...figma.tsx`<SidebarNavItem${_fcc_renderReactProp('level', level)}${_fcc_renderReactProp('expanded', expanded)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "level": "level2",
        "expanded": {
          "kind": "enum",
          "args": {
            "figmaPropName": "展开",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        }
      },
      "imports": [
        "import { SidebarNavItem } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=23715-10701",
    "label": "React",
    "language": "typescript",
    "component": "SidebarNavItem",
    "source": "",
    "sourceLocation": {
      "line": 29
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst level = 'level3'\nconst selected = figma.currentLayer.__properties__.enum('选中', {\n\"on\": true,\n\"off\": false})\nconst __props = {}\nif (level && level.type !== 'ERROR') {\n  __props[\"level\"] = level\n}\nif (selected && selected.type !== 'ERROR') {\n  __props[\"selected\"] = selected\n}\n\nexport default { ...figma.tsx`<SidebarNavItem${_fcc_renderReactProp('level', level)}${_fcc_renderReactProp('selected', selected)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "level": "level3",
        "selected": {
          "kind": "enum",
          "args": {
            "figmaPropName": "选中",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        }
      },
      "imports": [
        "import { SidebarNavItem } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  },
  {
    "figmaNode": "https://www.figma.com/design/JU4Freq42yQFn1GTe97pLy/SDS-%E9%80%9A%E7%94%A8-UI-%E7%BB%84%E4%BB%B6%E5%BA%93-v4.0.0?node-id=16586-179850",
    "label": "React",
    "language": "typescript",
    "component": "Button",
    "source": "",
    "sourceLocation": {
      "line": 13
    },
    "template": "function _fcc_renderReactProp(name, prop) {\n    // If the value is an array, then it's an array of objects representing React\n    // children (either of type INSTANCE for pills, or CODE for inline code). The\n    // template string handler in the template API handles extracting the instance\n    // objects in a way the UI can handle.\n    if (isReactComponentArray(prop)) {\n        if (prop.length > 1) {\n            // If the array has multiple children, render them wrapped in braces and a\n            // fragment.\n            //\n            // We recursively call `figma.tsx` on the value as it itself is an array of\n            // CODE/INSTANCE sections, so we need to run it through the template string\n            // function otherwise this would just output `[object Object]` for the value.\n            // The template string handler function handles flattening nested arrays.\n            return figma.tsx ` ${name}={<>${prop}</>}`;\n        }\n        else {\n            // Render a single child wrapped in braces, see above for why we use `figma.tsx`\n            return figma.tsx ` ${name}={${prop}}`;\n        }\n    }\n    // Render either the prop name or nothing for a boolean, we don't want to\n    // render `prop={true/false}`\n    if (typeof prop === 'boolean') {\n        return prop ? ` ${name}` : '';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"');\n        if (str === '') {\n            return '';\n        }\n        return ` ${name}=\"${str}\"`;\n    }\n    if (typeof prop === 'number') {\n        return ` ${name}={${prop}}`;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return ` ${name}={${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return ` ${name}={${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return ` ${name}={\\`${prop.$value}\\`}`;\n    }\n    return '';\n}\nfunction _fcc_renderReactChildren(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (typeof prop === 'string' || typeof prop === 'number' || typeof prop === 'boolean') {\n        return prop;\n    }\n    if (prop === undefined) {\n        return '';\n    }\n    if (prop.$type === 'template-string') {\n        // If the value is a template string, wrap in braces\n        return figma.tsx `{\\`${prop.$value}\\`}`;\n    }\n    // If the value is a JSX element, return it directly\n    if (prop.$type === 'jsx-element') {\n        return prop.$value;\n    }\n    // but for other values, wrap in braces\n    if (prop.$type === 'function' || prop.$type === 'identifier') {\n        return `{${prop.$value}}`;\n    }\n    if (prop.$type === 'array' || prop.$type === 'object') {\n        return `{${_fcc_renderPropValue(prop)}}`;\n    }\n    if (prop.$type === 'react-component') {\n        return `<${prop.$value} />`;\n    }\n}\nfunction _fcc_jsxElement($value) {\n    return {\n        $value,\n        $type: 'jsx-element',\n    };\n}\nfunction _fcc_function($value) {\n    return {\n        $value,\n        $type: 'function',\n    };\n}\nfunction _fcc_identifier($value) {\n    return {\n        $value,\n        $type: 'identifier',\n    };\n}\nfunction _fcc_object($value) {\n    return {\n        $value,\n        $type: 'object',\n        ...$value,\n    };\n}\nfunction _fcc_templateString($value) {\n    return {\n        $value,\n        $type: 'template-string',\n    };\n}\nfunction _fcc_renderPropValue(prop) {\n    if (isReactComponentArray(prop)) {\n        return prop;\n    }\n    if (prop === undefined) {\n        return 'undefined';\n    }\n    // Replace any newlines or quotes in the string with escaped versions\n    if (typeof prop === 'string') {\n        const str = `\"${prop.replaceAll('\\n', '\\\\n').replaceAll('\"', '\\\\\"')}\"`;\n        if (str === '') {\n            return 'undefined';\n        }\n        else {\n            return str;\n        }\n    }\n    if (typeof prop === 'boolean' || typeof prop === 'number') {\n        return prop;\n    }\n    if (prop.$type === 'function' ||\n        prop.$type === 'identifier' ||\n        prop.$type === 'jsx-element' ||\n        prop.$type === 'react-component') {\n        return prop.$value;\n    }\n    if (prop.$type === 'array') {\n        return `[${prop.$value.map((el) => _fcc_renderPropValue(el))}]`;\n    }\n    if (prop.$type === 'object') {\n        return `{${Object.keys(prop.$value)\n            .map((key) => ` ${key}: ${_fcc_renderPropValue(prop.$value[key])} `)\n            .join(',')}}`;\n    }\n    if (prop.$type === 'template-string') {\n        return `\\`${prop.$value}\\``;\n    }\n    return 'undefined';\n}\nfunction _fcc_stringifyObject(obj) {\n    if (Array.isArray(obj)) {\n        return `[${obj.map((element) => `${_fcc_stringifyObject(element)}`).join(',')}]`;\n    }\n    if (typeof obj !== 'object' || obj instanceof Date || obj === null) {\n        return JSON.stringify(obj);\n    }\n    return `{${Object.keys(obj)\n        .map((key) => ` ${key}: ${_fcc_stringifyObject(obj[key])} `)\n        .join(',')}}`;\n}\nfunction _fcc_reactComponent($value) {\n    return {\n        $value,\n        $type: 'react-component',\n    };\n}\nfunction _fcc_array($value) {\n    return {\n        $value,\n        $type: 'array',\n    };\n}\nfunction isReactComponentArray(prop) {\n    return (Array.isArray(prop) &&\n        prop.every((item) => item.type === 'INSTANCE' || item.type === 'CODE' || item.type === 'ERROR'));\n}\n\nconst figma = require('figma')\n\nconst label = figma.currentLayer.__properties__.string('按钮')\nconst type = figma.currentLayer.__properties__.enum('类型', {\n\"一级按钮\": 'primary',\n\"二级按钮\": 'default',\n\"白底按钮\": 'default',\n\"虚线按钮\": 'dashed',\n\"文本按钮\": 'text',\n\"链接\": 'link',\n\"图标按钮\": 'default'})\nconst size = figma.currentLayer.__properties__.enum('尺寸', {\n\"正常\": 'middle',\n\"小尺寸\": 'small'})\nconst kind = figma.currentLayer.__properties__.enum('种类', {\n\"标准\": 'standard',\n\"成功\": 'success',\n\"失败\": 'error'})\nconst disabled = figma.currentLayer.__properties__.enum('禁用', {\n\"on\": true,\n\"off\": false})\nconst loading = figma.currentLayer.__properties__.enum('加载中', {\n\"on\": true,\n\"off\": false})\nconst iconOnly = figma.currentLayer.__properties__.enum('类型', {\n\"一级按钮\": false,\n\"二级按钮\": false,\n\"白底按钮\": false,\n\"虚线按钮\": false,\n\"文本按钮\": false,\n\"链接\": false,\n\"图标按钮\": true})\nconst __props = {}\nif (label && label.type !== 'ERROR') {\n  __props[\"label\"] = label\n}\nif (type && type.type !== 'ERROR') {\n  __props[\"type\"] = type\n}\nif (size && size.type !== 'ERROR') {\n  __props[\"size\"] = size\n}\nif (kind && kind.type !== 'ERROR') {\n  __props[\"kind\"] = kind\n}\nif (disabled && disabled.type !== 'ERROR') {\n  __props[\"disabled\"] = disabled\n}\nif (loading && loading.type !== 'ERROR') {\n  __props[\"loading\"] = loading\n}\nif (iconOnly && iconOnly.type !== 'ERROR') {\n  __props[\"iconOnly\"] = iconOnly\n}\n\nexport default { ...figma.tsx`<Button${_fcc_renderReactProp('label', label)}${_fcc_renderReactProp('type', type)}${_fcc_renderReactProp('size', size)}${_fcc_renderReactProp('kind', kind)}${_fcc_renderReactProp('disabled', disabled)}${_fcc_renderReactProp('loading', loading)}${_fcc_renderReactProp('iconOnly', iconOnly)}/>`, metadata: { __props } }\n",
    "templateData": {
      "props": {
        "label": {
          "kind": "string",
          "args": {
            "figmaPropName": "按钮"
          }
        },
        "type": {
          "kind": "enum",
          "args": {
            "figmaPropName": "类型",
            "valueMapping": {
              "一级按钮": "primary",
              "二级按钮": "default",
              "白底按钮": "default",
              "虚线按钮": "dashed",
              "文本按钮": "text",
              "链接": "link",
              "图标按钮": "default"
            }
          }
        },
        "size": {
          "kind": "enum",
          "args": {
            "figmaPropName": "尺寸",
            "valueMapping": {
              "正常": "middle",
              "小尺寸": "small"
            }
          }
        },
        "kind": {
          "kind": "enum",
          "args": {
            "figmaPropName": "种类",
            "valueMapping": {
              "标准": "standard",
              "成功": "success",
              "失败": "error"
            }
          }
        },
        "disabled": {
          "kind": "enum",
          "args": {
            "figmaPropName": "禁用",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        },
        "loading": {
          "kind": "enum",
          "args": {
            "figmaPropName": "加载中",
            "valueMapping": {
              "on": true,
              "off": false
            }
          }
        },
        "iconOnly": {
          "kind": "enum",
          "args": {
            "figmaPropName": "类型",
            "valueMapping": {
              "一级按钮": false,
              "二级按钮": false,
              "白底按钮": false,
              "虚线按钮": false,
              "文本按钮": false,
              "链接": false,
              "图标按钮": true
            }
          }
        }
      },
      "imports": [
        "import { Button } from 'components';"
      ],
      "nestable": true
    },
    "metadata": {
      "cliVersion": "1.3.12"
    }
  }
]