import { createStyles } from 'antd-style';

export const useStyle = createStyles(() => {
    return {
        descriptionItem: {
            paddingBottom: 0,
            '.ant-descriptions-header': {
                marginBottom: '16px',
            },
        },
    };
});
