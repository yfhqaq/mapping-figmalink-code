"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getStorybookMetadata_exports = {};
__export(getStorybookMetadata_exports, {
  getStorybookMetadata: () => getStorybookMetadata
});
module.exports = __toCommonJS(getStorybookMetadata_exports);
var import_path = require("path");
var import_core_common = require("@storybook/core-common");
var import_getStorybookMain = require("./getStorybookMain");
const getStorybookMetadata = /* @__PURE__ */ __name(() => {
  const workingDir = (0, import_core_common.getProjectRoot)();
  const configDir = process.env.STORYBOOK_CONFIG_DIR ?? ".storybook";
  const main = (0, import_getStorybookMain.getStorybookMain)(configDir);
  const normalizedStoriesEntries = (0, import_core_common.normalizeStories)(main.stories, {
    configDir,
    workingDir
  }).map((specifier) => ({
    ...specifier,
    importPathMatcher: new RegExp(specifier.importPathMatcher)
  }));
  const storiesPaths = normalizedStoriesEntries.map((entry) => `${entry.directory}/${entry.files}`).map((dir) => (0, import_path.join)(workingDir, dir)).join(";");
  const lazyCompilation = !!main.core?.builder?.options?.lazyCompilation;
  return {
    configDir,
    workingDir,
    storiesPaths,
    normalizedStoriesEntries,
    lazyCompilation
  };
}, "getStorybookMetadata");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getStorybookMetadata
});
