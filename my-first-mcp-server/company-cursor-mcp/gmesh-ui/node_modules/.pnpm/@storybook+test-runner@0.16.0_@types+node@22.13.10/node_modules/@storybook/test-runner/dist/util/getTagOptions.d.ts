type TagOptions = {
    includeTags: string[];
    excludeTags: string[];
    skipTags: string[];
};
/**
 * Get filtering options based on:
 * 1. Test runner config 'tags' object
 * 2. Environment variables (takes precedence)
 */
declare function getTagOptions(): TagOptions;

export { getTagOptions };
