"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTextWidth = void 0;
/**
 * 基于 canvas.measureText api 计算并返回获取文字（指定字体）的 px 宽度
 * Uses canvas.measureText to compute and return the width of the given text of given font in pixels.
 * @param {String} text The text to be rendered.
 * @param {String} font The css font descriptor that text is to be rendered with (e.g. "bold 14px verdana").
 */
exports.getTextWidth = (function (t, font = '13px -apple-system, BlinkMacSystemFont, PingFang SC, Hiragino Sans GB, Microsoft YaHei, Segoe UI, Helvetica Neue, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji, Segoe UI Symbol') {
    // re-use canvas object for better performance
    const canvas = exports.getTextWidth.canvas || (exports.getTextWidth.canvas = document.createElement('canvas'));
    const context = canvas.getContext('2d');
    context.font = font;
    const metrics = context.measureText(t);
    return metrics.width;
});
//# sourceMappingURL=getTextWidth.js.map