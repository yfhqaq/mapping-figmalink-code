import { Dispatch, SetStateAction } from "react";
import { RouteObject, useLocation } from "react-router-dom";
/**
 * 配合 useState 来自动更新缓存
 * @param key 在独立的路由下不重复即可，生成时会使用 routeKey
 * @param defaultValue 等同于 useState(defaultValue)
 * @TODO 数据结构存在问题时的兜底方案
 */
export declare const useStateWithLocalCache: <S extends string | number | boolean | any[] | Record<string, any>>(key: string, defaultValue: SetStateAction<S>, routes: RouteObject[]) => [S, Dispatch<SetStateAction<S>>];
/**
 * 单纯的使用缓存，缓存的存取不会驱动视图更新
 * @param key 在独立的路由下不重复即可，生成时会使用 routeKey
 * @param defaultValue
 * @TODO 数据结构存在问题时的兜底方案
 */
export declare const useLocalCache: <S extends string | number | boolean | any[] | Record<string, any>>(key: string, defaultValue: SetStateAction<S>, routes: RouteObject[], location: ReturnType<typeof useLocation>) => {
    getCache: () => any;
    setCache: (data: S) => void;
    cacheKey: string;
};
//# sourceMappingURL=useCache.d.ts.map