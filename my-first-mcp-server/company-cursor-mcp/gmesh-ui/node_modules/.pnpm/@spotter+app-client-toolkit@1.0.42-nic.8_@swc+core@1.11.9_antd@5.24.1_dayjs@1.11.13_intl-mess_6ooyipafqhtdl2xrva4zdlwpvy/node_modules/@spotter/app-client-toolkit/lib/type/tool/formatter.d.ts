/**
 * CurrencyOption
 */
export type CurrencyOption = Partial<{
    /**
     * 保留的小数位数，默认保留2位
     */
    decimal: number;
    /**
     * 显示货币的模式。 默认“SD”
     * @pattern C: currency code，货币代码
     * @pattern S: currency symbol，货币符号
     * @pattern D: formatted digit，格式化后的数字
     */
    pattern: string;
    /**
     * 自定义空值Set，内置了默认值
     */
    emptySet: Set<any>;
    /**
     * 货币符号，默认$
     */
    symbol: string;
    /**
     * 默认的货币代码，默认USD
     */
    code: string;
}>;
/**
 * 单位格式化option
 * @pattern D: number，格式化后的数字
 * @pattern U: formatted digit，格式化后的数字
 */
export type UnitOption = {
    /** 单位，默认空字符串 */
    unit?: string;
    /** 自定义转化，默认不转化 */
    transform?<T = number>(n: T): T;
    /** 保留的小数位数，默认不保留 */
    decimal?: number;
    /**
     * 单位格式化支持的模板, 默认UD
     * @pattern D: formatted digit，格式化后的数字
     * @pattern U: unit，单位
     */
    pattern?: string;
};
/**
 * 货币模式支持的模板
 * @pattern C: currency code，货币代码
 * @pattern S: currency symbol，货币符号
 * @pattern D: formatted digit，格式化后的数字
 */
export type CurrencyPatternMap = {
    S: string;
    C: string;
    D: string;
};
//# sourceMappingURL=formatter.d.ts.map