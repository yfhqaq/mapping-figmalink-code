"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toCellWidth = exports.sortExceptConfig = exports.emptyToMinNumber = void 0;
const const_1 = require("../const");
/**
 * 空值转化为最小number
 * @return number
 */
const emptyToMinNumber = (v) => const_1.EMPTY_SET.has(v) || Number.isNaN(v) ? Number.MIN_SAFE_INTEGER : Number(v);
exports.emptyToMinNumber = emptyToMinNumber;
/**
 * 适用于columns中的排序sorter
 * @param dataIndex column中要比较的key
 * @param config 比较值配置，默认number比较
 */
const sortExceptConfig = (dataIndex, config) => (a, b, sortOrder) => {
    const { noSortedValue = ' ', type = 'number', noSortedKey } = { ...config };
    if (a[`${noSortedKey}`] === noSortedValue || `${a[dataIndex]}` === `${noSortedValue}`)
        return +(sortOrder === 'ascend');
    if (type === 'number')
        return (0, exports.emptyToMinNumber)(a[dataIndex]) - (0, exports.emptyToMinNumber)(b[dataIndex]);
    return +(a[dataIndex] > b[dataIndex]);
};
exports.sortExceptConfig = sortExceptConfig;
/**
 * 放在table中的宽度，与`PLATFORM_FIELD_REGEXP`的推荐宽度搭配
 * @param width PLATFORM_FIELD_REGEXP or 其推荐宽度
 * @param {*} iconWidth icon宽度，默认0
 * @param {*} paddingWidth table的padding总占宽度，默认16
 */
const toCellWidth = (width, iconWidth = 0, paddingWidth = 16) => (typeof width === 'number' ? width : width.recommendedWidth ?? 0) + iconWidth + paddingWidth;
exports.toCellWidth = toCellWidth;
//# sourceMappingURL=table.js.map