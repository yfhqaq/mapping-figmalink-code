"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.initSpotterI18n = exports.sptChangeLanguage = void 0;
const i18next_1 = __importDefault(require("i18next"));
const utils_1 = require("./utils");
const const_1 = require("./const");
// 提取i18n的changeLanguage
const originalChangeLanguage = i18next_1.default.changeLanguage;
/**
 * 切换项目翻译数据
 * @param customOption
 * @param source
 * @param callback
 * @param asyncComponentList  // 需要异步请求部件列表
 * @returns
 */
const sptChangeLanguage = (customOption, source = {}, callback, asyncComponentList = const_1.ASYNC_COMPONENT_LIST) => {
    const options = Object.assign(Object.assign({}, customOption), { 
        // 如果自定义参数无法获取到 domainUrl，则使用线上的 i18n api url 作为回退值
        domainUrl: customOption.domainUrl || const_1.DOMAIN_URL, 
        // 如果自定义参数无法获取到偏好语言，则使用 'zh' 作为回退值
        language: customOption.language || 'zh' });
    console.log('异步请求部件:', asyncComponentList);
    if (asyncComponentList === null || asyncComponentList === void 0 ? void 0 : asyncComponentList.size) {
        for (const component of customOption.componentList) {
            if (asyncComponentList.has(component)) {
                (0, utils_1.getLanguage)(Object.assign(Object.assign({}, options), { component }))
                    .then((res) => {
                    // 添加至i18n Bundle,覆盖模式
                    i18next_1.default.addResourceBundle(options.language, 'translation', Object.assign({}, res), true, true);
                    i18next_1.default.reloadResources(options.language);
                })
                    .catch((error) => {
                    console.error(error);
                });
            }
        }
    }
    return new Promise((resolve, reject) => {
        // 拉取当前项目所需翻译数据
        (0, utils_1.getProjectLanguage)(Object.assign(Object.assign({}, options), { componentList: (asyncComponentList === null || asyncComponentList === void 0 ? void 0 : asyncComponentList.size)
                ? customOption.componentList.filter((component) => !asyncComponentList.has(component))
                : customOption.componentList }))
            .then((data) => {
            // 添加至i18n Bundle,覆盖模式
            i18next_1.default.addResourceBundle(options.language, 'translation', Object.assign(Object.assign({}, source), data), true, true);
            // 拦截changeLanguage，通过接口返回数据灌入实例，再触发change
            resolve(originalChangeLanguage(options === null || options === void 0 ? void 0 : options.language, callback));
        })
            .catch((error) => reject(error));
    });
};
exports.sptChangeLanguage = sptChangeLanguage;
/**
 *
 * @param options i18n 初始化配置
 * @param source 预加载的数据
 * @returns
 */
const initSpotterI18n = (customOption, source = {}) => {
    // 需要异步请求的部件
    console.log('异步请求部件:', const_1.ASYNC_COMPONENT_LIST);
    const options = Object.assign(Object.assign({}, customOption), { 
        // 如果自定义参数无法获取到 domainUrl，则使用线上的 i18n api url 作为回退值
        domainUrl: customOption.domainUrl || const_1.DOMAIN_URL, 
        // 如果自定义参数无法获取到偏好语言，则使用 'zh' 作为回退值
        language: customOption.language || 'zh' });
    return new Promise((resolve, reject) => {
        const initializeI18n = () => __awaiter(void 0, void 0, void 0, function* () {
            try {
                // 同步请求部件
                const data = yield (0, utils_1.getProjectLanguage)(Object.assign(Object.assign({}, options), { 
                    // 过滤异步请求的部件
                    componentList: customOption.componentList.filter((component) => !const_1.ASYNC_COMPONENT_LIST.has(component)) }));
                // 初始化
                yield (0, utils_1.i18nInit)(options, Object.assign(Object.assign({}, source), data));
                // 异步请求部件
                for (const component of customOption.componentList) {
                    if (const_1.ASYNC_COMPONENT_LIST.has(component)) {
                        console.log('异步更新:', component);
                        (0, utils_1.asyncUpdateI18n)(options, component);
                    }
                }
                i18next_1.default.changeLanguage = (lng, callback) => (0, exports.sptChangeLanguage)(Object.assign(Object.assign({}, options), { language: lng }), source, callback, const_1.ASYNC_COMPONENT_LIST);
                resolve(i18next_1.default);
            }
            catch (initError) {
                console.error('获取资源存在错误', initError);
                // 失败时再次 init 防止子应用加载失败
                try {
                    yield (0, utils_1.i18nInit)(options, Object.assign({}, source));
                    resolve(i18next_1.default);
                }
                catch (i18nInitError) {
                    reject(i18nInitError);
                }
            }
        });
        initializeI18n();
    });
};
exports.initSpotterI18n = initSpotterI18n;
//# sourceMappingURL=init.js.map