import LRUCache from 'lru-cache';
import { Job, JobCallback } from 'node-schedule';
import BaseBackend, { IBackendOptions, ICallback, IData, IHeaders } from "./base";
declare class NodeBackend extends BaseBackend {
    job: Job;
    cache: LRUCache<string, IData>;
    constructor(options?: IBackendOptions<number>);
    fetch(url: string, headers: IHeaders, callback: ICallback<IData | undefined>): void;
    watch(cb: JobCallback): void;
    auth(url: string, callback: ICallback<string | undefined>): void;
    getCache(key: string, callback: ICallback<IData | undefined>): void;
    setCache(key: string, value: IData): void;
    resetCache(): void;
}
export default NodeBackend;
