"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
/**
 * 准确地更新浏览器的URL查询参数，防止URL被错误地拼接。
 *
 * @param {Object} params - 要更新或添加的查询参数对象。
 * @param {string} baseUrl - （可选）指定要更新查询参数的基础URL，默认为当前浏览器地址的路径部分。
 */
function updateSearchParams(params) {
  var baseUrl = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : window.location.protocol + '//' + window.location.host + window.location.pathname;
  // 解析基础URL
  var newUrl = new URL(baseUrl, window.location.origin);

  // 获取并修改当前的查询参数
  var searchParams = new URLSearchParams(newUrl.search);
  // 遍历并更新查询参数
  Object.keys(params).forEach(function (key) {
    if (params[key] === null || params[key] === undefined) {
      searchParams.delete(key); // 删除参数
    } else {
      searchParams.set(key, params[key]); // 更新或添加参数
    }
  });

  // 构造新的URL
  var updatedUrl = "".concat(newUrl.origin).concat(newUrl.pathname).concat(!params || Object.keys(params).length === 0 ? '' : '?').concat(searchParams.toString());

  // 使用history API更新浏览器地址栏，不刷新页面
  window.history.pushState({
    path: updatedUrl
  }, '', updatedUrl);
}
var _default = exports.default = updateSearchParams;
//# sourceMappingURL=index.js.map