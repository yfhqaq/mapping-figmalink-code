import { ProTableProps } from '@ant-design/pro-components';
import React from 'react';
export interface TableProps<DataSource, U, ValueType = 'text'> extends ProTableProps<DataSource, U, ValueType> {
    width?: string | number | true;
    fitClass?: string;
    ResizingHeaderCell?: any;
    cacheKeyWithRoute?: string;
    scrollY?: string;
    /**
     * 当选中了某段文字时触发
     */
    onRowSelection?: (selection: ReturnType<typeof document.getSelection>, event: React.MouseEvent<any, MouseEvent>) => void;
    /**
     * @param open
     * @param record
     * @description 除了下述情况，点击行时会执行该回调函数
     * 1. 选中了某段文字
     * 2. 点击的元素是 a 标签
     */
    onRowDrawerChange?: (open: boolean, record: DataSource) => void;
    ResizingIndicator?: any;
    /**
     * 适配模式
     * - auto: 自动适配，基于视窗高度 1080px 来启用不同的适配规则。
     * - content: 适配内容，即内容的高度优先，不设限制，表现为在溢出视窗高度的内容部分使用 sticky 模式下在 PageContainer 中滚动。
     * - container: 适配容器，即 table 容器的高度固定，内容溢出 table 容器的部分在 table body 内部滚动。
     * - none: 使用原始的 table 模式，不进行任何适配。
     */
    fit?: 'auto' | 'content' | 'container' | 'none';
    componentId: string;
}
declare function Table<DataSource, U, ValueType = 'text'>({ className, width, fit, fitClass, sticky, columns, cacheKeyWithRoute, onRow, onRowSelection, onRowDrawerChange, ResizingHeaderCell, components: originComponents, ResizingIndicator, scrollY, pagination, componentId: _componentId, ...restProps }: TableProps<DataSource, U, ValueType>): React.JSX.Element;
declare const _default: React.MemoExoticComponent<typeof Table>;
export default _default;
