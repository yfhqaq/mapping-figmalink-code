import { FC, ReactNode } from 'react';
interface BiEmbeddedProps {
    i18nKey: string;
    id: string;
    topSection?: ReactNode;
    action?: ReactNode;
}
export declare const BiEmbedded: FC<BiEmbeddedProps>;
export {};
