import { ProFormSelectProps, ProSchema, RequestOptionsType } from '@ant-design/pro-components';
import { DefaultOptionType } from 'antd/es/select';
import React, { ReactNode } from 'react';
export interface SelectBaseProps<T, U> extends Omit<ProFormSelectProps, 'request' | 'params'> {
    /**
     * 业务类型
     */
    request: (...params: T[]) => Promise<U[]>;
    viewMode?: 'light' | 'normal';
    /**
     * 可以控制业务下拉组件的request请求参数，实现自定义请求参数
     */
    params?: ProSchema<T>['params'] | T;
    format?: (value: U[]) => RequestOptionsType[];
    labelName: keyof U | ((values: U) => ReactNode);
    valueName: keyof U | ((values: U) => string | number | undefined);
}
export declare function runPropFunction<U>(prop: keyof U | ((values: U) => ReactNode), item: U): DefaultOptionType['label'] | Exclude<DefaultOptionType['value'], null>;
export declare const runFormat: (value: any[], format?: ((value: any[]) => any[]) | undefined) => any[];
/**
 * 业务下拉组件基础方法，合并所有业务类型的下拉组件
 */
declare const SelectBase: <T, U>({ request: customRequest, viewMode, params, fieldProps, ...props }: SelectBaseProps<T, U>) => React.JSX.Element;
export default SelectBase;
