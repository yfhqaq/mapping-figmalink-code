import { TabPaneProps } from 'antd';
import React, { FC, ReactElement, ReactNode } from 'react';
import { SptTabBadgeType } from '../SptTabWithBadge';
import './style/index.less';
export declare const SptPageContainerContext: React.Context<{
    tableAction?: React.ReactNode[] | undefined;
} | null>;
export declare const SptPageContainerProvider: React.Provider<{
    tableAction?: React.ReactNode[] | undefined;
} | null>;
export interface InnerTabsProps {
    label: ReactNode;
    key: string;
    count?: number;
}
export interface SptPageContainerProps {
    /**
     * 页面标题
     */
    title?: ReactNode;
    /**
     * 页面副标题
     */
    subTitle?: ReactNode;
    /**
     * tab页签
     */
    tabs?: (TabPaneProps & {
        count?: number;
    })[];
    /**
     * tab页签角标类型，默认为corner
     */
    tabBadgeType?: SptTabBadgeType;
    /**
     * 基础路径, 用于tab路由模式作为基础路径
     */
    basePath?: string;
    /**
     * header的额外区域
     */
    extra?: ReactElement;
    /**
     * 右侧操作区域
     */
    action?: ReactNode;
    /**
     * 表格操作区域，适用于列表页面，会自动将tableAction的内容放在接收表格的操作区域最右侧
     */
    tableAction?: ReactNode[];
    /**
     * 容器加载
     */
    loading?: boolean;
    /**
     * 底部工具栏，使用fixed操作固定在页面对底部，适用于编辑、新增页面
     */
    footerToolbar?: ReactNode;
    /**
     * 顶部插槽区域
     */
    topSection?: ReactNode;
    /**
     * Tab 路由模式, 默认为true， 为false时可关闭tab路由模式
     */
    tabNavigationMode?: boolean;
    /**
     * Tab 切换事件
     * @param activeTabKey
     * @returns
     */
    onTabChange?: (activeTabKey: string) => void;
    /**
     * 内容的布局适配容器
     */
    fitContainer?: boolean;
    /**
     * 自动默认tab key
     */
    autoDefaultTabKey?: boolean;
    children?: ReactNode | undefined;
}
declare const SptPageContainer: FC<SptPageContainerProps>;
export default SptPageContainer;
