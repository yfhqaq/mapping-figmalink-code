import { BadgeProps, TabsProps } from 'antd';
import { FC, ReactNode } from 'react';
import './style/index.less';
export declare enum SptTabBadgeType {
    Inline = 0,
    Corner = 1
}
export interface SptWithBadgeTabProps {
    key: string;
    label: string | ReactNode;
    children?: ReactNode;
    count?: number;
}
export interface SptTabWithBadgeProps extends TabsProps {
    items?: SptWithBadgeTabProps[];
    badgeType?: SptTabBadgeType;
    badgeProps?: BadgeProps;
}
/**
 * 带Badge的Tab菜单
 */
declare const SptTabWithBadge: FC<SptTabWithBadgeProps>;
export default SptTabWithBadge;
