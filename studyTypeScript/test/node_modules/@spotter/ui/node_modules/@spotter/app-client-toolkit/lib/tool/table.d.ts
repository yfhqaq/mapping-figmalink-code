import { PLATFORM_FIELD_REGEXP } from '../const';
import { IdentifierType, SortExceptConfigType } from '../type';
/**
 * 空值转化为最小number
 * @return number
 */
export declare const emptyToMinNumber: (v?: IdentifierType) => number;
/**
 * 适用于columns中的排序sorter
 * @param dataIndex column中要比较的key
 * @param config 比较值配置，默认number比较
 */
export declare const sortExceptConfig: SortExceptConfigType;
/**
 * 放在table中的宽度，与`PLATFORM_FIELD_REGEXP`的推荐宽度搭配
 * @param width PLATFORM_FIELD_REGEXP or 其推荐宽度
 * @param {*} iconWidth icon宽度，默认0
 * @param {*} paddingWidth table的padding总占宽度，默认16
 */
export declare const toCellWidth: (width: number | (typeof PLATFORM_FIELD_REGEXP)[keyof typeof PLATFORM_FIELD_REGEXP], iconWidth?: number, paddingWidth?: number) => number;
//# sourceMappingURL=table.d.ts.map