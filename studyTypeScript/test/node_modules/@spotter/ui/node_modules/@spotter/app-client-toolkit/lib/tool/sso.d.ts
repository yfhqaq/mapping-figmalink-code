declare const enum ENV {
    local = "local",
    development = "development",
    test = "test",
    production = "production"
}
export declare const SessionKeys = "spotter_token";
export declare const AuthErrorCode: Set<number>;
export declare const PermissionErrorCode: Set<number>;
interface SsoParams {
    env: keyof typeof ENV;
    appCode: string;
    loginPath?: string;
    ssoUrl?: string;
}
/**
 * 纯前端使用
 */
export declare class SSOSDK {
    private env;
    private appCode;
    private readonly SSO_TICKET;
    private readonly SPOTTER_SSO_REDIRECT_COUNT;
    private readonly SPOTTER_SSO_START_TIME;
    private loginPath;
    private ssoUrl;
    constructor({ env, ssoUrl, appCode, loginPath }: SsoParams);
    login(): Promise<void>;
    private doLoginByTicket;
    private checkLogin;
    getLastSpotterSsoTicket(url: string): null;
    removeQueryParam(url: string | URL, param: string): string;
    redirectToSSO(): void;
    redirectToSSOLogout(): void;
    redirectWithLimit(limit?: number): void;
    getSSOToken(): string;
    private getRedirectUrl;
}
/**
 * 存在前后端使用
 */
export declare class SSOSDKClient {
    private env;
    private readonly SSO_TICKET;
    private appCode;
    constructor({ env, appCode }: SsoParams);
    login(): void;
    logout(): void;
    clearCookie(name: string): void;
    removeQueryParam: (url: string | URL, param: string) => string;
    getCookie(): string;
}
export {};
//# sourceMappingURL=sso.d.ts.map