export interface WithPagination<T = any> {
    currentPage: number;
    pageSize: number;
    totalNum: number;
    totalPage: number;
    startIndex: number;
    isMore: boolean;
    data: T[];
}
export interface _I18nTextModel {
    /**
     * 翻译文案key
     * @type {string}
     */
    'i18nKey'?: string;
    /**
     * 文案动态参数
     * @type {{ [key: string]: object; }}
     */
    'params'?: {
        [key: string]: object;
    };
    /**
     * 默认展示文案，国际化引擎失效时的兜底方案
     * @type {string}
     */
    'defaultValue'?: string;
}
export interface SpotterResponseResultSchema<T = any> {
    code: number;
    data: T;
    msg: string | _I18nTextModel;
}
export type RetType<T> = T extends (...args: any) => Promise<infer U> ? U : T;
export type GetRetType<T> = RetType<T> extends SpotterResponseResultSchema<infer P> ? P : never;
export type GetPageRetType<T> = GetRetType<T> extends WithPagination<infer P> ? P : never;
//# sourceMappingURL=returnType.d.ts.map