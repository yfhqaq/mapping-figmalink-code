"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.localCache = exports.LocalCache = void 0;
const isObject_1 = __importDefault(require("lodash/isObject"));
/**
 * 基于 LocalStorage 的本地缓存类
 */
class LocalCache {
    set(key, value) {
        window.localStorage.setItem(key, (0, isObject_1.default)(value) ? JSON.stringify(value) : value.toString());
    }
    get(key) {
        const s = window.localStorage.getItem(key);
        try {
            return JSON.parse(s);
        }
        catch {
            return s;
        }
    }
    has(key) {
        return !!window.localStorage.getItem(key);
    }
    remove(key) {
        window.localStorage.removeItem(key);
    }
}
exports.LocalCache = LocalCache;
exports.localCache = new LocalCache();
//# sourceMappingURL=localCache.js.map