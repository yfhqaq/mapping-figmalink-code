import { SpotterMainApp } from "../type";
import { GlobalLayoutType, SpotterRouteObject } from "../../router";
import { FunctionComponent } from "react";
export interface RouterSystemOptions {
    layout?: {
        [key in GlobalLayoutType]: FunctionComponent;
    };
    guard?: {
        element?: FunctionComponent;
        downgradeRoutes?: SpotterRouteObject[];
    };
}
export declare function boostrapRouterSystem(options: RouterSystemOptions, sptApp: SpotterMainApp): void;
//# sourceMappingURL=index.d.ts.map