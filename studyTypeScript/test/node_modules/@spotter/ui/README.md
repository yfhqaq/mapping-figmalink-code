# Spotter UI Component Library

# How to develop this project?

1. Run this command `pnpm run storybook` in the root of spotter-ui
2. And, you have to run `node index.js` in another project `spotter-ui-server` root path, which gonna start a proxy server to get cookie.

