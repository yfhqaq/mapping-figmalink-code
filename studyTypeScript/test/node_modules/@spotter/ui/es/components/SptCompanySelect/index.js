var __defProp = Object.defineProperty;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};

// src/components/SptCompanySelect/index.tsx
import React from "react";
import { ProFormSelect } from "@ant-design/pro-components";
import { companySdk } from "../../services/CompanySdk";
import { SptComponentProvider } from "../Provider";
var SptCompanySelect = (_a) => {
  var _b = _a, {
    noStyle = true,
    allowClear = true,
    showSearch = true,
    fieldProps,
    filter,
    debounceTime,
    valueField = "id",
    onLoaded
  } = _b, props = __objRest(_b, [
    "noStyle",
    "allowClear",
    "showSearch",
    "fieldProps",
    "filter",
    "debounceTime",
    "valueField",
    "onLoaded"
  ]);
  return /* @__PURE__ */ React.createElement(SptComponentProvider, null, /* @__PURE__ */ React.createElement(
    ProFormSelect,
    __spreadValues({
      noStyle,
      allowClear,
      fieldProps: __spreadValues({}, fieldProps),
      request: (_0) => __async(void 0, [_0], function* ({ keyWords }) {
        const { data: res } = yield companySdk.listAll({
          businessCodeOrName: keyWords
        });
        onLoaded == null ? void 0 : onLoaded(res.data);
        return (filter ? res.data.filter((item) => filter(item)) : res.data).map(
          (e) => ({
            label: `${e.name} (${e.businessCode})`,
            value: e[valueField]
          })
        );
      }),
      debounceTime: debounceTime != null ? debounceTime : 500,
      placeholder: "请输入",
      showSearch
    }, props)
  ));
};
var SptCompanySelect_default = SptCompanySelect;
export {
  SptCompanySelect_default as default
};
//# sourceMappingURL=index.js.map
