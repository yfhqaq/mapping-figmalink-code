import { Tooltip } from 'antd';
import { ComponentProps, FC, ReactNode } from 'react';
import './style/index.less';
export interface SptHelpTooltipProps extends Omit<ComponentProps<typeof Tooltip>, 'title' | 'className'> {
    help: ReactNode | (() => ReactNode);
    className?: string;
}
/**
 * 带QuestionIcon的Tooltip
 * @param props
 * @returns
 */
declare const SptHelpTooltip: FC<SptHelpTooltipProps>;
export default SptHelpTooltip;
