// src/components/SptTable/utils.ts
var toFloat = (n) => +n.toFixed(2);
var clearSelection = () => {
  const selection = window.getSelection();
  if (selection) {
    if (selection.empty) {
      selection.empty();
    } else if (selection.removeAllRanges) {
      selection.removeAllRanges();
    }
  }
};
export {
  clearSelection,
  toFloat
};
//# sourceMappingURL=utils.js.map
