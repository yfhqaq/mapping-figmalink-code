var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));

// src/components/SptTable/Form/Submitter.tsx
import { UndoOutlined } from "@ant-design/icons";
import { useSptTranslation } from "@spotter/i18n-sdk";
import { Button } from "antd";
import Omit from "omit.js";
import React, { memo } from "react";
var Submitter = (props) => {
  const {
    // onSubmit,
    // onReset,
    // form,
    submit,
    reset,
    // render,
    searchConfig = {},
    submitButtonProps,
    resetButtonProps
  } = props;
  if (props.render === false) {
    return null;
  }
  const { t } = useSptTranslation();
  const { submitText = t("common.action_query"), resetText = t("common.action_reset") } = searchConfig;
  const renderDom = [];
  if (submitButtonProps !== false) {
    renderDom.push(
      /* @__PURE__ */ React.createElement(
        Button,
        __spreadProps(__spreadValues({
          key: "submit"
        }, Omit(submitButtonProps || {}, ["preventDefault"])), {
          type: "primary",
          onClick: (e) => {
            var _a;
            if (!(submitButtonProps == null ? void 0 : submitButtonProps.preventDefault)) {
              submit == null ? void 0 : submit();
            }
            (_a = submitButtonProps == null ? void 0 : submitButtonProps.onClick) == null ? void 0 : _a.call(
              submitButtonProps,
              e
            );
          }
        }),
        submitText
      )
    );
  }
  if (resetButtonProps !== false) {
    renderDom.push(
      /* @__PURE__ */ React.createElement(
        Button,
        __spreadProps(__spreadValues({
          type: "link",
          icon: /* @__PURE__ */ React.createElement(UndoOutlined, null)
        }, Omit(resetButtonProps || {}, ["preventDefault"])), {
          key: "reset",
          onClick: (e) => {
            var _a;
            if (!(resetButtonProps == null ? void 0 : resetButtonProps.preventDefault)) {
              reset == null ? void 0 : reset();
            }
            (_a = resetButtonProps == null ? void 0 : resetButtonProps.onClick) == null ? void 0 : _a.call(
              resetButtonProps,
              e
            );
          }
        }),
        resetText
      )
    );
  }
  if (Array.isArray(renderDom)) {
    if ((renderDom == null ? void 0 : renderDom.length) < 1) {
      return null;
    }
    if ((renderDom == null ? void 0 : renderDom.length) === 1) {
      return renderDom[0];
    }
    return /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "8px" } }, renderDom);
  }
  return renderDom;
};
Submitter.displayName = "Submitter";
var Submitter_default = memo(Submitter);
export {
  Submitter_default as default
};
//# sourceMappingURL=Submitter.js.map
