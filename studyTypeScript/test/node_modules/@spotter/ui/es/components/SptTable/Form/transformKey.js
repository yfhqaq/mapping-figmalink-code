// src/components/SptTable/Form/transformKey.ts
import { isNil } from "@ant-design/pro-components";
var transformKey = (values, searchKeyMap) => {
  if (typeof values !== "object" || isNil(values) || values instanceof Blob) {
    return values;
  }
  const transformedValues = {};
  for (const key in values) {
    if (Object.prototype.hasOwnProperty.call(values, key)) {
      const element = values[key];
      if (isNil(element)) {
        continue;
      }
      const transformFunc = searchKeyMap[key];
      if (transformFunc) {
        const newKeyMap = transformFunc(element);
        Object.assign(transformedValues, newKeyMap);
      } else {
        transformedValues[key] = element;
      }
    }
  }
  return transformedValues;
};
var transformKey_default = transformKey;
export {
  transformKey_default as default
};
//# sourceMappingURL=transformKey.js.map
