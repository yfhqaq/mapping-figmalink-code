import { FC } from 'react';
import { SptLinkType } from '../SptLink';
import { SptIconProps } from '../SptIcon';
export interface SptOuterLinkProps {
    type?: SptLinkType;
    to: string;
    text: string;
    sptIconProps?: SptIconProps;
}
/**
 * 跳转外部地址，带Icon版本的SpotterLink
 * @param props
 * @returns
 */
declare const SptOuterLink: FC<SptOuterLinkProps>;
export default SptOuterLink;
