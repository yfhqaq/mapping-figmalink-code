import { SelectProps } from 'antd';
import { CSSProperties, FC, ReactText } from 'react';
import './style/index.less';
export type SptSelectOption = {
    label: string;
    value: string | number;
    [key: string]: any;
};
export interface SptSelectProps {
    style?: CSSProperties;
    className?: string;
    loading?: boolean;
    title?: string;
    options?: SptSelectOption[];
    value?: ReactText[];
    tooltip?: string;
    placeholder?: string;
    request?: () => Promise<SptSelectOption[]>;
    onChange?: (checkedOptions: ReactText[]) => void;
}
/**
 * Spotter Select的封装 (后期打算作为Select组件的统一出口)
 * 带Transfer的下拉选择框,需搭配FormItem或者ProFormItem使用
 * @param param0
 * @returns
 */
declare const SptSelect: FC<SptSelectProps & SelectProps>;
export default SptSelect;
