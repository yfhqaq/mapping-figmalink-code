import { ModalFormProps } from '@ant-design/pro-components';
import { CSSProperties, FC, ReactElement, ReactText } from 'react';
export interface SptRestModalProps {
    style?: CSSProperties;
    className?: string;
    title?: string;
    trigger?: ReactElement;
    options?: ReactText[];
    placeholder?: string;
    open?: boolean;
    modalProps?: ModalFormProps;
}
/**
 * 即将废弃，这看起来是一个临时组件
 */
declare const SptRestModal: FC<SptRestModalProps>;
export default SptRestModal;
