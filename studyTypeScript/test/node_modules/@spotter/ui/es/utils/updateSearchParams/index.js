// src/utils/updateSearchParams/index.ts
function updateSearchParams(params, baseUrl = window.location.protocol + "//" + window.location.host + window.location.pathname) {
  const newUrl = new URL(baseUrl, window.location.origin);
  const searchParams = new URLSearchParams(newUrl.search);
  Object.keys(params).forEach((key) => {
    if (params[key] === null || params[key] === void 0) {
      searchParams.delete(key);
    } else {
      searchParams.set(key, params[key]);
    }
  });
  const updatedUrl = `${newUrl.origin}${newUrl.pathname}${!params || Object.keys(params).length === 0 ? "" : "?"}${searchParams.toString()}`;
  window.history.pushState({ path: updatedUrl }, "", updatedUrl);
}
var updateSearchParams_default = updateSearchParams;
export {
  updateSearchParams_default as default
};
//# sourceMappingURL=index.js.map
