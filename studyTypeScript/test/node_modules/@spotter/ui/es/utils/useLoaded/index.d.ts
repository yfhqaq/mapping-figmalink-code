import { DependencyList, EffectCallback } from 'react';
/**
 * useLoaded
 *
 * @description 拓展useEffect，不在first render时执行
 */
export declare const useLoadedEffect: (fn: EffectCallback, deps: DependencyList) => void;
