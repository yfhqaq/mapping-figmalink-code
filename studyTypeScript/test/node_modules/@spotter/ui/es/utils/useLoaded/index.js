// src/utils/useLoaded/index.ts
import { useEffect, useRef } from "react";
var useLoadedEffect = (fn, deps) => {
  const isFirst = useRef(true);
  useEffect(() => {
    if (isFirst.current) {
      isFirst.current = false;
      return;
    }
    return fn();
  }, deps);
};
export {
  useLoadedEffect
};
//# sourceMappingURL=index.js.map
